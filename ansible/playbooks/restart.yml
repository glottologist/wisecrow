---
- name: Restart Wisecrow services (no rebuild)
  hosts: calypso
  vars_files: [../vars/secrets.yml]
  tasks:
    - ansible.builtin.command:
        cmd: docker compose -f {{ compose_file }} restart
        chdir: "{{ deploy_dir }}"
      become: true
