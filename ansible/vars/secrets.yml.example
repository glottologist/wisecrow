---
# Encrypt with: ansible-vault encrypt vars/secrets.yml
# Edit with:    ansible-vault edit    vars/secrets.yml

# Database
postgres_password: "REPLACE_ME_DB_PASSWORD"

# LLM provider (gloss, graded-reader, generate-exercises, seed-grammar).
# Leave blank if unused.
llm_api_key: ""

# Stock image API keys (learning cards). Leave blank to disable images.
# Set IMAGE_PROVIDER in inventory/group_vars/all.yml if needed
# (auto | unsplash | pexels | pixabay). Default app behaviour is auto.
unsplash_api_key: ""
pexels_api_key: ""
pixabay_api_key: ""

# Sync server auth (validates inbound /sync/* requests). Leave blank if
# this instance does not accept sync clients.
sync_api_key_secret: ""

# Per-host sudo password (referenced from inventory/hosts.yml as
# ansible_become_password).
become_pw_calypso: "REPLACE_ME_CALYPSO_SUDO_PW"

# deSEC API token for DNS-01 issuance (desec.io -> Token Management). The
# challenge hook uses it to write the _acme-challenge TXT in the delegation
# zone (wisecrow_desec_domain). Leave blank if wisecrow_tls_obtain_cert is
# false.
wisecrow_desec_token: ""
