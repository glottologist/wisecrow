[package]
name = "wisecrow-dto"
version = "0.1.0"
edition = "2021"
description = "WASM-safe shared types for Wisecrow"

[dependencies]
chrono = { version = ">=0.4,<0.4.44", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
